{
  "version": "1.0.0",
  "description": "Machine-readable source of truth for Alcheris interactive blocks, mobile behavior, and learning beats. Human docs (references/*.md) and the native in-app agent prompt both derive from this file. Shapes verified against the frontend renderer/widget code.",
  "generated_from": [
    "frontend/src/features/block/BlockRenderer.jsx",
    "frontend/src/features/block/interactionblock/modes/*.jsx",
    "frontend/src/features/block/illustrationblock/illustrationUtils.js",
    "frontend/src/features/block/comparisonblock/ComparisonRenderer.jsx",
    "frontend/src/features/block/canvasblock/CanvasRenderer.jsx",
    "frontend/src/features/block/artifact/ArtifactBlock.jsx",
    "frontend/src/features/block/artifact/artifactContract.js",
    "frontend/src/features/player/mobileLearningBeats.js",
    "frontend/src/features/player/MobileLessonFlow.jsx",
    "frontend/src/features/player/playerBlockVisibility.js",
    "frontend/src/features/player/blockAnswered.js"
  ],
  "pedagogical_spine": {
    "flow": "hook -> realize -> understand -> apply -> reflect",
    "hook": "The engagement opener: 'why should I care?' - a relatable scene, stakes, a curiosity gap, or a 'by the end you'll...' promise, in persona. Distinct from realize; earns attention before any content. Usually an article opener with one inline interactive pull.",
    "realize": "The learner notices the phenomenon via an interactive element (drag a slider, watch an animation, notice prompt) BEFORE any rule is stated.",
    "understand": "Give the rule, model, and worked example; scannable - bold key terms, callouts for the core rule/trap, bullets or an accordion for multi-point content, with inline widgets.",
    "apply": "Guided practice (varied types) + real production. Light checks stay INLINE in the article; a sustained production task (essay/coding/exam) becomes a split pinned workspace.",
    "reflect": "A short article closer in persona: what you can now do, the one big idea, a next step.",
    "format": "Teaching flows as INTERACTIVE ARTICLES (single column, interactions and quick checks woven inline - the explorable-explanation format). Use SPLIT/pinned-workspace layout only when a sustained task needs guidance kept beside it (essay/coding/data-lab/exam). Do not split-ify ordinary teaching.",
    "rule": "Teach before you test: never place a question/task about a rule before it is hooked, realized, and understood; never open a page with a wall of text."
  },
  "authoring_rules": {
    "emphasis": "Bold key terms; put core rules, traps, and warnings in callouts. Do not bold whole sentences, over-highlight, or use ALL CAPS.",
    "multi_point": "Explanations covering three or more parallel points, rules, items, or steps use a bullet list or an accordion, not a dense paragraph. Text blocks support bullet lists and markdown bold.",
    "exercise_variety": "Do not make every exercise multiple choice. Mix quiz types short_answer / multiple_select / cloze and standalone sequence / cloze / flashcard / comparison blocks, matched to the skill (recognition->quiz, ordering->sequence, precise wording->cloze, recall->flashcard, weak-vs-strong->comparison).",
    "answer_distribution": "Vary the correct-answer position in multiple choice. NEVER default the correct option to A (correctAnswerIndex 0). Order options naturally, not answer-first, and distribute correct answers across A/B/C/D roughly evenly across the lesson. Every-answer-is-A is a broken lesson.",
    "no_duplicate_practice": "Do not duplicate practice across pages. Each page's exercises must be distinct in content and type; never repeat the same question, reuse a block type with the same material, or give two pages an identical practice structure.",
    "production": "Every taught skill needs at least one task where the learner PRODUCES it (short_answer for a sentence, essay for longer writing, custom_activity for structured tasks, artifact for custom interactive HTML/CSS/JS learning objects). For language/writing, test USING the language, e.g. 'write one sentence describing this trend using a strong verb', not only choosing a definition.",
    "vocabulary": "Do not dump vocabulary or long reference lists into one flat table. Use flashcard decks grouped by meaning, grouped accordions, or compact grouped lists, and pair a vocabulary bank with at least one usage exercise.",
    "illustration_placement": "An illustration always goes in the RIGHT panel using illustration mode as the single right-panel block; it demands space.",
    "persona": "Every lesson has a teaching persona (voice); write ALL learner-facing text in it consistently (hook, explanations, feedback, callouts). Teachers pick a preset or define their own; if the user names a character/vibe, build a custom persona from it. Voice never changes the pedagogy, only how it sounds, and it NEVER shortens the teaching - keep full explanatory depth, at least one worked example, and the why (not just the what). Punchy personas (Dante/high-energy) must be cool AND thorough, not reduce explanations to one-liners. Keep it classroom-appropriate. See references/alcheris-teacher-personas.md.",
    "interactive_article": "Single-column pages are interactive articles: weave interactions and quick checks INLINE into the narrative (drag-to-reveal graph, animation, inline quiz/cloze, try-this widget). Make this the default for teaching; reserve split layout for sustained workspaces (essay/coding/data-lab/exam).",
    "engagement_hook": "Open every lesson with a why-should-I-care hook (relatable scene, stakes, curiosity gap, 'by the end you'll...'), in persona, with one inline interactive pull - before any content.",
    "progressive_disclosure": "Keep the main path clean; put depth (the why, proofs, edge cases, history, fast-finisher challenges) in 'go deeper' accordions and side callouts, not the main flow."
  },
  "engagement_patterns": [
    "Reveal before explain: let the learner uncover the trend or curve first, then teach it.",
    "Manipulate before tell: let the learner move a slider and form a hypothesis before stating the rule.",
    "Watch before summarize: play the transformation, then state the takeaway."
  ],
  "interactive_first_rules": [
    { "signal": "data changes over time", "use": "interaction.graph", "instead_of": "static image chart" },
    { "signal": "spread / average / variance / parameter concept", "use": "interaction.distribution", "instead_of": "static curve image plus prose" },
    { "signal": "a function or 'what if we change X'", "use": "interaction.equation", "instead_of": "static formula plus one example" },
    { "signal": "a process or transformation to watch", "use": "illustration", "instead_of": "a bullet list of steps" },
    { "signal": "a before/after change is the point", "use": "comparison", "instead_of": "two stacked images or blocks" },
    { "signal": "a diagram built step by step", "use": "canvas", "instead_of": "one finished diagram image" },
    { "signal": "concepts and their relationships", "use": "mindmap", "instead_of": "a flat list" }
  ],
  "media_in_practice_blocks": {
    "note": "Quiz questions and flashcards accept images, but agents usually omit them and ship text-only. Include an image whenever a visual aids the question or the recall - especially chart/diagram questions and vocabulary/object flashcards. Reuse an existing lesson image URL or an uploaded asset URL.",
    "quiz_question_image": "Each object in content.questions may include image: '<url>', rendered above the question. Use it when the question refers to a chart, diagram, map, or picture.",
    "flashcard_card_image": "Each object in content.cards may include image: '<url>' and imagePosition: 0-100 (vertical focal point, default 50). Use images for vocabulary, objects, places, and visual recall."
  },
  "blocks": {
    "interaction": {
      "engagement": "interactive",
      "mobile_behavior": "full",
      "mode_field": "content.mode",
      "modes": {
        "graph": {
          "purpose": "Animated line graph; a slider progressively reveals data points so the learner uncovers the trend.",
          "prefer_over": "A static image chart whenever data changes across time and the learner should notice the movement.",
          "content_schema": {
            "mode": "graph",
            "graphTitle": "string",
            "xAxisLabel": "string",
            "yAxisLabel": "string",
            "showFullGraph": "boolean (true shows the whole line at once; use for reference/model pages)",
            "graphData": "array of { id:string, label:string (x tick), value:number (y), annotation?:string } for a single line",
            "series": "OPTIONAL alternative to graphData for MULTIPLE lines: array of { label:string, color?:hex, points:[{label,value,annotation?}] } - drawn with a legend and shared slider reveal"
          },
          "example": {
            "mode": "graph",
            "graphTitle": "UK telephone calls by category, 1995-2002",
            "xAxisLabel": "Year",
            "yAxisLabel": "Billions of minutes",
            "showFullGraph": false,
            "graphData": [
              { "id": "p1", "label": "1995", "value": 70 },
              { "id": "p2", "label": "1999", "value": 90, "annotation": "Peak" },
              { "id": "p3", "label": "2002", "value": 70 }
            ]
          },
          "cautions": ["Do not put the answer in annotation on a point shown before a quiz about it."]
        },
        "distribution": {
          "purpose": "Live normal-distribution curve reshaped by mean and standard-deviation sliders.",
          "prefer_over": "A static curve image plus prose when teaching spread, averages, or variance.",
          "content_schema": {
            "mode": "distribution",
            "statTitle": "string",
            "statInstruction": "string",
            "statMean": "number",
            "statStdDev": "number",
            "allowStudentInteraction": "boolean (false locks the sliders to teacher-set values)"
          },
          "example": {
            "mode": "distribution",
            "statTitle": "How spread changes the curve",
            "statInstruction": "Drag the sliders to see how mean and standard deviation reshape the data.",
            "statMean": 50,
            "statStdDev": 10,
            "allowStudentInteraction": true
          }
        },
        "equation": {
          "purpose": "Function grapher from an editable expression with variable sliders; gameMode makes a match-the-curve puzzle.",
          "prefer_over": "A static formula and single example when teaching functions, transformations, or parameters.",
          "content_schema": {
            "mode": "equation",
            "mathExpression": "string, e.g. 'a * x^2 + b'",
            "mathA": "number", "mathB": "number", "mathC": "number",
            "mathXMin": "number", "mathXMax": "number", "mathYMin": "number", "mathYMax": "number",
            "mathAllowUserEdit": "boolean (false locks the expression)",
            "showLinkedTable": "boolean (show synced numeric X/Y table)",
            "gameMode": "boolean",
            "targets": "array of { x:number, y:number } (puzzle target points when gameMode is true)"
          },
          "example": {
            "mode": "equation",
            "mathExpression": "a * x^2 + b",
            "mathA": 1, "mathB": 0, "mathC": 0,
            "mathXMin": -10, "mathXMax": 10, "mathYMin": -10, "mathYMax": 10,
            "mathAllowUserEdit": true,
            "showLinkedTable": true,
            "gameMode": false,
            "targets": [ { "x": -5, "y": 5 }, { "x": 0, "y": 0 }, { "x": 5, "y": 5 } ]
          }
        },
        "embed": {
          "purpose": "External interactive web resource inside the interaction frame.",
          "content_schema": { "mode": "embed", "embedUrl": "string" }
        }
      }
    },
    "illustration": {
      "engagement": "interactive",
      "mobile_behavior": "viewer",
      "purpose": "Keyframed multi-scene visual explanation; scenes auto-play and auto-advance. Use when a process or transformation is best shown in motion.",
      "prefer_over": "A bullet list of steps when motion or staged reveal is the teaching object.",
      "content_schema": {
        "version": 1,
        "stage": { "width": "number", "height": "number", "backgroundColor": "hex", "fit": "contain|cover" },
        "scenes": "array of { id, title, durationMs, layers:[layer], keyframes:{ [layerId]: [keyframe] } }",
        "layer": "{ id, type:'shape|text|image|svg', x,y,width,height, and type-specific: shape('shape' e.g. rect), text('text'), image/svg('src'); style: opacity,rotation,scaleX,scaleY,fill,stroke,color,fontSize,borderRadius }",
        "keyframe": "{ timeMs:number, easing:'linear|easeIn|easeOut|easeInOut', props:{ target values } }"
      },
      "cautions": ["Keep on-screen text short.", "Verify no text/visual overlap in the player."]
    },
    "comparison": {
      "engagement": "interactive",
      "mobile_behavior": "viewer",
      "purpose": "Before/after: image slider, text word-diff, or code line-diff.",
      "prefer_over": "Two stacked images or blocks when the change itself is the lesson.",
      "type_field": "content.compareType",
      "content_schema": {
        "compareType": "image | code | text",
        "height": "number",
        "image": "beforeImage, afterImage (urls); optional beforeAnnotations/afterAnnotations = { markers:[], drawings:[] }",
        "text": "beforeText, afterText (strings)",
        "code": "beforeCode, afterCode (strings)"
      },
      "example": { "compareType": "text", "height": 450, "beforeText": "The graph go up.", "afterText": "The graph rose steadily." }
    },
    "canvas": {
      "engagement": "interactive",
      "mobile_behavior": "viewer",
      "purpose": "Replayable drawing; plays back paths like a hand drawing live, or a student sketch surface.",
      "prefer_over": "One finished diagram image when the build order teaches something.",
      "content_schema": {
        "height": "number",
        "backgroundColor": "hex",
        "backgroundPattern": "plain | (other patterns)",
        "paths": "array (stroke data authored in the visual editor)",
        "images": "array",
        "speed": "number",
        "autoplay": "boolean",
        "showReplayButton": "boolean",
        "instantReplay": "boolean"
      }
    },
    "mindmap": {
      "engagement": "interactive",
      "mobile_behavior": "viewer",
      "purpose": "Node/relationship map for concept maps, brainstorms, and relationship structures.",
      "content_schema": {
        "nodes": "array of { id:string, x:number, y:number, label:string, type?:'root', color?:hex }"
      },
      "example": { "nodes": [ { "id": "root", "x": 300, "y": 150, "label": "Central idea", "type": "root", "color": "#3b82f6" } ] }
    },
    "chart": {
      "engagement": "presentation",
      "mobile_behavior": "full",
      "purpose": "Responsive data chart (line, bar, area, pie) with multiple series and optionally multiple charts per block. Use to PRESENT data figures. Use interaction.graph instead when you want a drag-to-reveal exploration; use chart when you just need to show the data (especially multi-series, which interaction.graph also supports but chart renders more richly).",
      "content_schema": {
        "chartType": "line | bar | area | pie",
        "title": "string",
        "xKey": "field name in each data row for the x-axis/category (default 'label'; pie uses 'name')",
        "series": "array of { key:string (a field in the data rows), label?:string, color?:hex }",
        "data": "array of row objects: line/bar/area -> { [xKey]:string, <seriesKey>:number, ... }; pie -> { name:string, value:number }",
        "charts": "OPTIONAL array of full chart configs to render several charts in one block"
      },
      "example": {
        "chartType": "line", "title": "UK telephone calls, 1995-2002", "xKey": "year",
        "series": [ { "key": "local", "label": "Local fixed-line", "color": "#4C72B0" }, { "key": "mobile", "label": "Mobiles", "color": "#55A868" } ],
        "data": [ { "year": "1995", "local": 70, "mobile": 3 }, { "year": "2002", "local": 70, "mobile": 50 } ]
      }
    },
    "artifact": {
      "engagement": "interactive",
      "mobile_behavior": "content.mobileBehavior.mode (full | viewer | simplified | desktop_recommended | desktop_required)",
      "purpose": "Sandboxed HTML/CSS/JS learning object, like a Notion-style artifact inside the Alcheris editor/player. Use when native blocks cannot express the custom interface, simulation, drag/drop tool, or reusable micro-app.",
      "prefer_over": "A pile of static text/images or an unsafe custom_activity when the learner needs a custom interactive object.",
      "content_schema": {
        "type": "artifact",
        "title": "string",
        "description": "optional string",
        "version": "semantic version string, e.g. 1.0.0",
        "runtime": "html_sandbox",
        "entry": "file path present in files, e.g. /index.html",
        "files": "object of path -> text content; include /index.html and optional /styles.css /script.js",
        "renderTargets": "{ editorBlock:true, fullPanel:true, playerDesktop:true, playerMobile:true }",
        "mobileBehavior": "{ mode:'full|viewer|simplified|desktop_recommended|desktop_required', minWidth?:number, layout?:'responsive|fixed' }",
        "completion": "{ type:'none|manual|on_interaction|on_submit' }",
        "submission": "{ enabled:boolean, mode:'none|draft_only|submit_required|draft_and_submit|teacher_review_required|score_required', storage:'alcheris_managed' }",
        "analytics": "{ events:['block_viewed','block_interacted','block_completed', ...] }",
        "security": "{ sandbox:true, allowNetwork:false, allowClipboard:boolean, allowStorage:false }",
        "reuse": "{ scope?:'lesson|teacher|community', tags?:string[] }"
      },
      "bridge": {
        "track": "window.AlcherisArtifact.track(action, payload)",
        "complete": "window.AlcherisArtifact.complete(payload)",
        "submit": "window.AlcherisArtifact.submit(answer)",
        "resize": "window.AlcherisArtifact.resize(height)"
      },
      "example": {
        "type": "artifact",
        "title": "Sentence Sorter",
        "version": "1.0.0",
        "runtime": "html_sandbox",
        "entry": "/index.html",
        "files": {
          "/index.html": "<main><h1>Sentence Sorter</h1><button id=\"done\">Done</button></main>",
          "/styles.css": "body { font-family: system-ui; }",
          "/script.js": "document.getElementById('done').onclick = () => window.AlcherisArtifact.complete({ score: 1 });"
        },
        "renderTargets": { "editorBlock": true, "fullPanel": true, "playerDesktop": true, "playerMobile": true },
        "mobileBehavior": { "mode": "full", "minWidth": 320, "layout": "responsive" },
        "completion": { "type": "on_interaction" },
        "submission": { "enabled": false, "mode": "none", "storage": "alcheris_managed" },
        "analytics": { "events": ["block_viewed", "block_interacted", "block_completed"] },
        "security": { "sandbox": true, "allowNetwork": false, "allowClipboard": false, "allowStorage": false }
      },
      "cautions": [
        "Do not include credentials, private URLs, auth tokens, or hidden tracking.",
        "Do not use eval-style code or remote scripts to bypass review.",
        "Do not access parent windows, cookies, local storage, or teacher storage directly.",
        "Keep allowNetwork false in beta; networked artifacts require a reviewed deployment setting.",
        "Artifact library writes are teacher/admin-only; students may only interact and submit through the bridge.",
        "Do not mark mobileBehavior.mode full unless the artifact is actually responsive and touch-friendly.",
        "In guided flow, the artifact MUST call window.AlcherisArtifact.submit or .complete at least once to release the answer gate; otherwise the learner is trapped."
      ]
    }
  },
  "mobile_behavior_defaults": {
    "full": ["text", "paragraph", "quote", "callout", "accordion", "image", "video", "gallery", "quiz", "cloze", "sequence", "flashcard", "checkpoint", "interaction", "artifact(responsive)", "coding(inline)", "essay(<=600 words)"],
    "viewer": ["canvas", "mindmap", "comparison", "illustration", "pdf"],
    "desktop_recommended": ["data-lab", "embed", "artifact(complex)", "coding(immersive)", "essay(>600 words)"],
    "override_field": "content.mobileBehavior",
    "override_values": ["full", "viewer", "simplified", "desktop_recommended", "desktop_required"]
  },
  "learning_beats": {
    "definition": "A teaching unit one layer above blocks (setup -> model -> practice -> feedback). Mobile always renders as a sequence of beats; desktop learners can also toggle a guided flow that reveals blocks one at a time using the same engine.",
    "auto_generation": {
      "override": "If page.mobileBeatsOverride is a non-empty array, it is used verbatim.",
      "single_layout": "Split on headings; every activity block closes the current beat.",
      "split_layout": "Each right-panel activity anchors one beat; left blocks attach by heading section (if >1 heading) or by even chunking; extra left groups become trailing beats."
    },
    "authoring_rules": [
      "Signpost with one heading per teaching point so beats break where meaning breaks.",
      "Keep each right-panel activity next to (or in the same heading section as) the explanation it practices.",
      "For a split page with N right activities, provide about N left heading sections (rough 1:1).",
      "Order blocks setup -> model -> practice top to bottom within a panel.",
      "One dominant activity per beat; split with a heading if a beat mixes unrelated activities.",
      "For mobile-native interactivity prefer interaction widgets (full); treat canvas/illustration/comparison/mindmap as watch-only on phones. Artifact blocks follow content.mobileBehavior.mode and should be full only when responsive and touch-friendly.",
      "Every block must stand alone in guided flow: guided flow reveals one block at a time with a Continue button between them. Do not rely on the surrounding blocks being visible for context; put framing in the block's title, heading, or first sentence."
    ],
    "override_shape": {
      "mobileBeatsOverride": [
        { "id": "beat-1", "label": "Introduce variables", "blockIds": ["text-a", "text-b", "quiz-a"] },
        { "id": "beat-2", "label": "Practice in code", "blockIds": ["text-c", "coding-a"] }
      ]
    }
  },
  "guided_flow": {
    "description": "Guided flow reveals blocks one at a time with a Continue button. It is always on for mobile, and desktop learners can toggle it in the player header (Layers button). Same beat engine as mobile - authoring rules from learning_beats apply directly.",
    "answer_gated_types": ["quiz", "cloze", "sequence", "essay", "custom_activity", "artifact", "interaction"],
    "answer_gate_contract": "In guided flow, an answer-gated block HARD-BLOCKS progression until the learner completes it (the block dispatches a 'block-answered' event). Every gated block must be completable end-to-end: quiz has >=1 valid question, cloze has bracketed answers + wordBank, sequence has >=2 real items, essay has a prompt, custom_activity has a working submit step, artifact calls window.AlcherisArtifact.submit or .complete, interaction is resolvable (slider reaches end, equation puzzle has a valid target). A gated block that cannot be completed silently traps the learner. Prefer non-gated equivalents (text/callout/image/illustration/chart) for decorative blocks.",
    "empty_block_filter": "The player silently filters empty text/paragraph/h1-h6/quote/code blocks (playerBlockVisibility.js). Do not ship placeholder-only text/heading blocks and assume they will be visible - they won't."
  },
  "sequential_order": {
    "description": "Courses have a course-level `linear_progression` flag (Sequential order in CourseManager) and per-lesson `unlock_requirements`. When linear_progression is on, students must finish each lesson to unlock the next in the shown order. Per-lesson requirements add extra prereq lessons on top.",
    "authoring_implication": "In a sequential-order course, EVERY lesson AND every gated block must be completable start-to-finish. A broken quiz or an artifact that never emits completion stops the student not only inside the lesson but from opening the next one. Verify lesson completability whenever the target course has linear_progression set."
  },
  "teacher_assets": {
    "description": "Teachers own an asset library at /teacher/assets (backend endpoint /api/assets/) for reusable images, video, PDFs, DOCX, Markdown, SVG, and other teaching files. Assets are Alcheris-managed URLs - artifacts and blocks reference them by URL.",
    "authoring_rules": [
      "Prefer teacher-library assets over one-off image_search for media that recurs across lessons (course logo, subject-specific diagrams, worked-example images).",
      "For an artifact that needs a media file, use a teacher-asset URL rather than an external URL - the sandbox has network off by default and only reaches Alcheris-managed origins.",
      "Uploaded teacher assets still need credit fields when they originate from an attributed source (Pexels/Wikimedia)."
    ]
  },
  "student_notebook": {
    "description": "Students own a personal notebook (StudentNotebook + StudentNotebookItem in backend) where they can save any lesson block into their own review workspace. Each notebook item stores a frozen source_snapshot of the block at fork time - teacher edits to the lesson do NOT retroactively change a student's saved copy. Notebook items can also carry student_state (their answers, interaction values, submission), and are the future substrate for AI-personalized review paths and NotebookLM-style source-pack export.",
    "authoring_implication": "Author every block to be MEANINGFUL WHEN DETACHED from the surrounding page: a callout carries its own rule, a worked example carries problem + solution, a flashcard teaches standalone, an interaction includes title + observation prompt inside the widget. Blocks that only make sense next to their neighbours become unusable notebook items. This is the same rule as the guided-flow one-block-at-a-time reveal - both flatten out contextual dependencies.",
    "forkable_kinds": ["note", "forked_block", "forked_interaction", "forked_artifact", "attempt", "generated_review", "asset", "external_source"]
  },
  "checkpoint_block": {
    "description": "The checkpoint block is the designed staged-reveal gate: use it when a page should PAUSE and force the learner to reveal/confirm/compare before continuing (staged answer reveal, sync/compare state, recovery point). Its future guided-flow role extends per-block trigger rules to code/data-lab/artifact and desktop-heavy blocks (pass-check / manual continue / desktop-required notice) - author with that intent even where the runtime is not yet wired.",
    "not_currently_in_answer_gate_list": "checkpoint is NOT yet in guided_flow.answer_gated_types (which reflects what the running player dispatches). Design intent is to add it. Do not rely on checkpoint hard-blocking progression today, but do use it to signal the pedagogical intent of a gated moment."
  }
}
